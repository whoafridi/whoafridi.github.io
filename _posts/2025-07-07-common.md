---
layout: post
title: React Testing Library user-event Cheat Sheet
subtitle: বাস্তব ইউজারের মতো টেস্ট লিখুন
tags: [javascript, react, testing]
permalink: /react-user-event-cheat-sheet/
---

👉 ইউজার যেভাবে ব্যবহার করে, ঠিক সেভাবেই টেস্ট করুন

`fireEvent` কাজ করে, কিন্তু একটু রোবটের মতো।  
আর `user-event` হলো **একদম মানুষের মতো**—টাইপ করা, ক্লিক করা, ট্যাব দেওয়া সবকিছু।

---

## কেন user-event ব্যবহার করবেন?

- টাইপ করলে এক অক্ষর এক অক্ষর করে যায়
- focus / blur নিজে থেকেই হ্যান্ডেল করে
- বাস্তব ইউজার বিহেভিয়ারের কাছাকাছি

---

## সেটআপ

```javascript
import userEvent from "@testing-library/user-event";

const user = userEvent.setup();
```

````

---

## সবচেয়ে কমন ব্যবহার

### বাটনে ক্লিক

```javascript
await user.click(button);
```

### ইনপুটে টাইপ

```javascript
await user.type(input, "hello world");
```

### ইনপুট ক্লিয়ার

```javascript
await user.clear(input);
```

### চেকবক্স

```javascript
await user.click(checkbox);
```

### ড্রপডাউন (select)

```javascript
await user.selectOptions(select, "admin");
```

---

## fireEvent বনাম user-event (মনে রাখার মতো)

❌ fireEvent → টেকনিক্যাল
✅ user-event → বাস্তব

👉 নতুন প্রজেক্টে **সবসময় user-event ব্যবহার করুন**

---

````

---

## 2️⃣ রিয়েল লাইফ ফর্ম টেস্টিং (Login Form উদাহরণ)

````markdown
layout: post
title: React Login Form কীভাবে টেস্ট করবেন
subtitle: বাস্তব উদাহরণ দিয়ে ধাপে ধাপে
tags: [javascript, react, testing]
permalink: /react-login-form-testing/

---

# React Login Form টেস্ট করা (Real Example)

ফর্ম টেস্টিং মানেই আসল পরীক্ষা।
কারণ এখানেই সবচেয়ে বেশি বাগ হয়।

ধরুন—

- Email ইনপুট
- Password ইনপুট
- Login বাটন

---

## কী কী টেস্ট করব?

✅ ইউজার টাইপ করতে পারছে কিনা
✅ ভুল ইনপুটে error দেখাচ্ছে কিনা
✅ ঠিক ইনপুটে submit হচ্ছে কিনা

---

## উদাহরণ টেস্ট

```javascript
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import LoginForm from "./LoginForm";

test("ইউজার ঠিকভাবে লগইন করতে পারে", async () => {
  const user = userEvent.setup();
  render(<LoginForm />);

  const email = screen.getByLabelText("Email");
  const password = screen.getByLabelText("Password");
  const button = screen.getByRole("button", { name: /login/i });

  await user.type(email, "test@email.com");
  await user.type(password, "123456");
  await user.click(button);

  expect(screen.getByText("Login successful")).toBeInTheDocument();
});
```
````

👉 এই টেস্টটা পড়লেই বোঝা যায়—ইউজার কী করছে।

---

## কমন ভুল

❌ ইনপুট না ভরে submit করা
❌ error message টেস্ট না করা
❌ async জিনিসে wait না করা

---

````

---

## 3️⃣ React Testing Interview Cheat Sheet 🎯

```markdown
layout: post
title: React Testing Interview Cheat Sheet
subtitle: প্রশ্ন, উত্তর আর বাস্তব ধারণা
tags: [javascript, react, testing]
permalink: /react-testing-interview-cheat-sheet/
---
# React Testing Interview Cheat Sheet

ইন্টারভিউতে শুধু কোড নয়—**ভাবনাটা** জানতে চায়।

---

## Q1: React Testing Library কেন ব্যবহার করবেন?

👉 কারণ এটা implementation না,
👉 **user behaviour** টেস্ট করে।

---

## Q2: fireEvent আর user-event এর পার্থক্য?

- fireEvent → low level
- user-event → real user simulation

👉 Best practice: **user-event**

---

## Q3: getByRole কেন গুরুত্বপূর্ণ?

👉 Accessibility + user perspective
👉 Screen reader যেভাবে দেখে, সেভাবে টেস্ট

---

## Q4: কখন getByTestId ব্যবহার করবেন?

👉 যখন role বা text পাওয়া যায় না
👉 last option হিসেবে

---

## Q5: এক টেস্টে কত কাজ করা উচিত?

👉 একটাই
👉 এক test = এক behaviour

---

## Q6: async টেস্টে কী ব্যবহার করবেন?

```javascript
await waitFor(...)
findBy...
````

👉 data আসার আগে expect লিখবেন না

---

## Q7: কী টেস্ট করবেন না?

❌ internal state
❌ private function
❌ CSS class

---

## ইন্টারভিউ গোল্ডেন লাইন 😄

> “I test the application the way a real user uses it.”

বলতে পারলে ভালো impression পড়ে।

---

```

---

## 🎁 শেষ কথা

আপনার কাছে এখন আছে—

✅ React Testing Cheat Sheet
✅ user-event Guide
✅ Real Form Testing Example
✅ Interview Ready Notes

```
